# Gems que necesita `generar_feeds.rb` a través del crawler vendoreado en
# `vendor/bounty-targets/`. `base64` dejó de venir con Ruby 3.4.
#
#   gem install --user-install -g Gemfile
source "https://rubygems.org"

gem "base64"
gem "kramdown"
gem "nokogiri"
gem "ssrf_filter"
gem "twingly-url"
